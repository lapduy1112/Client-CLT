import React from "react";
import MainLayout from "../MainLayout";
import { Container, Typography } from "@mui/material";

const Home = () => {
  return (
    <MainLayout>
      <Container maxWidth="lg" className="mt-8"></Container>
    </MainLayout>
  );
};

export default Home;
